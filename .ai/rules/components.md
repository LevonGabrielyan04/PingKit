---
paths:
  - resources/js/components/MonitorForm.vue
---

# Components

## MonitorForm uses NeoBrutalismCSS
MonitorForm uses NeoBrutalismCSS classes (nb-card, nb-input, nb-dropdown, nb-textarea, nb-checkbox, nb-button, nb-radio, nb-form-group), not shadcn/reka form controls. Prefer the orange variant to match project-color-theme.
